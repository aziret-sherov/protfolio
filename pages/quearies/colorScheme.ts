import { useMutation, QueryCache } from 'react-query';

const queryCache = new QueryCache();

const useSaveColorScheme = () => {
    // const saveColorScheme = async (colorScheme) => {
    //     queryCache.setQueryData('colorScheme', {
    //         background: ,
    //     });
    //     return savedColorScheme;
    // };
    //
    // return useMutation(saveColorScheme);
};

export default useSaveColorScheme;
